import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  createButton: {
    padding: 18,
    width: 52,
    height: 52,
    backgroundColor: '#1E6F9F',
    borderRadius: 6,
    top: -40,
  },
});
